---

# 📌 Fiche pratique – CASE WHEN

## 🔹 Définition

`CASE WHEN` permet d’ajouter des conditions dans une requête SQL.
👉 C’est l’équivalent d’un **if / elif / else** en programmation.
Très utile pour créer des colonnes calculées ou appliquer des règles de catégorisation.

---

## 🔹 Syntaxe SQL

```sql
CASE
    WHEN condition1 THEN valeur1
    WHEN condition2 THEN valeur2
    ...
    ELSE valeur_par_defaut
END
```

---

## 🔹 Exemple SQL

Supposons une table `ventes` avec une colonne `montant` :

```sql
SELECT montant,
       CASE
           WHEN montant < 50 THEN 'Petit achat'
           WHEN montant BETWEEN 50 AND 200 THEN 'Moyen achat'
           ELSE 'Gros achat'
       END AS categorie
FROM ventes;
```

📊 Résultat : ajoute une colonne `categorie` selon le montant.

---

## 🔹 Équivalent en Python (pandas)

```python
import pandas as pd

# Exemple DataFrame
ventes = pd.DataFrame({"montant": [20, 75, 300]})

# Equivalent CASE WHEN avec numpy.select
import numpy as np

conditions = [
    ventes["montant"] < 50,
    ventes["montant"].between(50, 200),
]
valeurs = ["Petit achat", "Moyen achat"]

ventes["categorie"] = np.select(conditions, valeurs, default="Gros achat")

print(ventes)
```

📊 Résultat : ajoute une colonne "categorie" comme en SQL.

---

✅ **À retenir** :

* `CASE WHEN` = conditions dans une requête.
* Super pratique pour transformer, classifier, créer des colonnes.
* En Python → `numpy.select` ou `np.where` pour reproduire le même effet.

---

